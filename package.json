{
  "name": "mcp-keycloak-internal-oidc-demo",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build ./src/server.ts --outdir ./dist --target bun",
    "test": "bun test"
  }
}
