# MCP Notes

The HTTP MCP endpoint remains `POST /mcp`.

Implemented JSON-RPC methods:

- `initialize`
- `notifications/initialized`
- `tools/list`
- `tools/call`

Initial tools remain `whoami`, `echo`, and `admin_status`.
